export type TProducts = {
    image: string,
    name: string,
    brand: string,
    available_quantity: number,
    price: number,
    rating: number,
    description: string,
    isDeleted?: boolean,
}